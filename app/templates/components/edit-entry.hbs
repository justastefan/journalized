{{#if title}}
  <h1 id="title">{{title}}</h1>
{{/if}}
{{#if description}}
  <p>{{description}}</p>
{{/if}}

{{#if errorMessage}}
  <div class="alert alert-danger">
    {{errorMessage}}
  </div>
{{/if}}
<form class="form-horizontal" {{action "save" on="submit"}}>

{{!--
  <div class="form-group">
    <label for="image" class="col-sm-2 control-label">Avatar</label>
    <div class="col-sm-10">
      {{#x-file-input multiple=true action=(action (mut files)) accept="image/gif,image/jpg,image/png"}}
        <button class="btn btn-primary">⇧ Choose Files</button>
      {{/x-file-input}}

      <div class="media">
        {{#each files as |file|}}
          {{#x-object-url blob=file as |url|}}
          <div class="media-left">
              <div style="background-image: url({{url}})" class="media-object image-preview" alt="file preview"></div>

          </div>
          <div class="media-body">
            <h4 class="media-heading">{{file.name}}</h4>
            <p>Type: {{file.type}}, size: {{file.size}}</p>
          </div>
          {{/x-object-url}}
        {{/each}}
      </div>
      {{#each files as |file|}}
        <div class="file-upload">
          {{#x-object-url blob=file as |url|}}
            <div style="background-image: url({{url}})" class="image-preview pull-left" alt="file preview"></div>
            <div class="image-file-name">{{file.name}}, {{file.type}}, {{file.size}}</div>
          {{/x-object-url}}
        </div>
      {{/each}}
    </div>
  </div>

  --}}


  <div class="form-group">
    <label for="input-creation-date" class="col-sm-2 control-label">Date</label>
    <div class="col-sm-10">
       {{input value=edit.creationDate class="form-control" id="input-creation-date" placeholder="Date"}}
    </div>
  </div>
  <div class="form-group">
    <label for="input-title" class="col-sm-2 control-label">Title</label>
    <div class="col-sm-10">
      {{input value=edit.title placeholder="" class="form-control" id="input-title"}}
    </div>
  </div>
  <div class="form-group">
    <label for="input-memo" class="col-sm-2 control-label">Memo</label>
    <div class="col-sm-10">
      {{textarea value=edit.memo placeholder="Write down things that made you smile, your thoughts, ideas, inspirations." class="form-control" id="input-memo"}}
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <div class="checkbox">
        <label>
          {{input type="checkbox" checked=edit.public}} Public
        </label>
      </div>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      {{#if model}}
        <button type="submit" class="btn btn-success">Save</button>
      {{else}}
        <button type="submit" class="btn btn-success">Create</button>
      {{/if}}
      {{#if model}}
        <button class="btn btn-danger" {{action "delete"}}>Delete</button>
      {{/if}}
      <a class="btn btn-link" {{action "cancel"}}>Cancel</a>
    </div>
  </div>
</form>
